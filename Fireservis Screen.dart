import 'package:flutter/cupertino.dart';

class Fireservis extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Fireservis"),
      ),
    );
  }

}